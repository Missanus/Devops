---

apiVersion: v1
kind: Service
metadata:
  name: lb-server-service
  labels:
    app: nginx
spec:
  ports:
    - name: "http"
      protocol: TCP
      port: 80
      targetPort: 80
  #clusterIP: None
  selector:
    app: nginx-lb
    zone: 'fr'
---
